# Rate limiting zone for the VPN credentials page.
# Installed to /etc/nginx/conf.d/vpn-ratelimit.conf by setup-nginx.sh.
# Must be in nginx's http{} context — loaded before sites-enabled.
limit_req_zone $binary_remote_addr zone=vpn_auth:10m rate=5r/m;
limit_req_status 429;
