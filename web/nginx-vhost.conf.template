server {
    listen 80;
    server_name ${CREDENTIALS_DOMAIN};

    location / {
        auth_basic "VPN";
        auth_basic_user_file /etc/nginx/htpasswd-vpn;
        root ${WEBROOT};
        index index.html;
    }
}
