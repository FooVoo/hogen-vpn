# WebSocket upgrade map — used by the Grafana and WGDashboard reverse proxies.
# Installed to /etc/nginx/conf.d/vpn-netdata-proxy.conf by setup-nginx.sh.
# Must be in nginx's http{} context — loaded before sites-enabled.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      keep-alive;
}
